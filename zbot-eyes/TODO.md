# TODO

## GUI + Segmentation
- [ ] Verify GUI launches on laptop
  - Done when: `python -m net_inspector.gui` opens the window.
- [ ] Install PyTorch CPU wheels
  - Done when: segmentation weights load without errors.
- [ ] Live camera segmentation
  - Done when: live camera is smooth and segmentation FPS is visible.

## Data + Demo
- [ ] Generate demo images with sky/debris/tear/fire
  - Done when: GUI can generate and segment a demo image.
- [ ] Save overlay outputs
  - Done when: `outputs/annotated/` contains segmentation overlays.

